package notes.section04

fun main() {
// From 29

// Int, Long, Short, Char, Float, Byte
// like java, Long can have suffix L, Float can have f
// numbers are Int by default
// convert values to type X with .toX()

// Any class: check section03/Declarations.kt
// Unit class: singleton, mostly used for return void
// Nothing class: a subclass of every class, used for when a function will never return ex. inf loop
}